import React from 'react';
import Layout from '../../layout';
import SchedulerLayout from './Scheduler_Childs/SchedulerLayout';

function Scheduler() {
  return (
      <Layout>
          <SchedulerLayout />
      </Layout>
  );
}

export default Scheduler;
